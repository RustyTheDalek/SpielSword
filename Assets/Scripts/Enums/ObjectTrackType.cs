public enum ObjectTrackType
{
    FrameTracking, //Tracks changes on a per frame basis (can also include event tracking)
    EventTracking, //Just tracks events useful for adjusting components at certain points
};
